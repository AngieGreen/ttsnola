<%= form_for(@blogging) do |f| %>
  <% if @blogging.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blogging.errors.count, "error") %> prohibited this blogging from being saved:</h2>

      <ul>
      <% @blogging.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :blog_text %><br>
    <%= f.text_area :blog_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
